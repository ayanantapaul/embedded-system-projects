how to flash code to an arduino nano using just the terminal(arduino-cli)?

step-1: 
	$ cd <path_to_project_folder>
step-2:
	$ arduino-cli compile -b arduino:avr:nano ./ && arduino-cli upload -p /dev/ttyUSB0 -b arduino:avr:nano ./
